# Neon Postgres connection string (POOLED — recommended for serverless)
# Find this in your Neon Console → "Connection Details" → "Pooled connection".
# Replace YOUR_PASSWORD_HERE with the actual password.
#
# Tip: NEVER commit a real .env file. The .gitignore already excludes it.

DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD_HERE@ep-jolly-cloud-ascdo9r2-pooler.c-4.eu-central-1.aws.neon.tech/neondb?sslmode=require
